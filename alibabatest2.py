import sys

# def main(path, favor):

if __name__ == "__main__":
    # cards = [1,0,0,1]
    # main(cards)
    # t = int(next(sys.stdin))
    # path = []
    # for i in range(t):
    #     h1, h2, length = next(sys.stdin).split(" ")
    #     h1, h2, length = int(h1), int(h2), int(length)
    #     path.append([h1, h2, length])

    # favor = []
    # for i in range(3):
    #     l = next(sys.stdin).split(" ")
    #     f = [int(s) for s in l[1:]]
    #     favor.append(f)
    # main(path, favor)